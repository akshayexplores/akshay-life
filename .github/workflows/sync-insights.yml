name: Sync content from akshay-brain

on:
  # Trigger when akshay-brain pushes to main (via repository_dispatch from akshay-brain workflow)
  repository_dispatch:
    types: [sync-insights]
  # Also run nightly as a fallback
  schedule:
    - cron: '0 3 * * *'
  # Allow manual trigger
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout akshay-life
        uses: actions/checkout@v4

      - name: Checkout akshay-brain
        uses: actions/checkout@v4
        with:
          repository: akshayexplores/akshay-brain
          path: akshay-brain
          token: ${{ secrets.BRAIN_ACCESS_TOKEN }}

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Sync Insights
        run: |
          python scripts/sync-insights.py

      - name: Mark synced insights as published
        run: |
          python scripts/mark-published.py

      - name: Sync site content (projects, tools, build log)
        run: |
          python scripts/sync-site-content.py

      - name: Drift check
        # Reports unmapped subjects, a stale build log and ignored draft
        # status. Never fails the sync - the log is the signal.
        continue-on-error: true
        run: |
          python scripts/check-drift.py

      - name: Commit and push changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add content/
          if git diff --cached --quiet; then
            echo "No changes to commit"
          else
            git commit -m "Sync content from akshay-brain"
            git push
          fi
